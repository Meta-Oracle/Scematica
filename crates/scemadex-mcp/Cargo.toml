[package]
name = "scemadex-mcp"
version = "0.1.0"
edition = { workspace = true }
authors = { workspace = true }
license = { workspace = true }
description = "Model Context Protocol (MCP) server exposing the ScemaDEX agentic-liquidity rail — reputation, pool-score, advice, bonded inference quotes, and experience purchase — to any LLM agent, monetized over x402."
repository = "https://github.com/meta-oracle/scematica"
homepage = "https://github.com/meta-oracle/scematica"
readme = "README.md"
keywords = ["mcp", "solana", "x402", "agent", "llm"]
categories = ["command-line-utilities", "api-bindings"]

[[bin]]
name = "scemadex-mcp"
path = "src/main.rs"

[dependencies]
tokio = { workspace = true }
serde = { workspace = true }
serde_json = { workspace = true }
reqwest = { workspace = true }
clap = { workspace = true }
anyhow = { workspace = true }
tracing = { workspace = true }
tracing-subscriber = { workspace = true }
